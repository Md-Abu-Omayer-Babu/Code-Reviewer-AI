from services.classFinder import classFinder

# Provide the full path to the example.py file
file_path = r"db/example.py"

# Call the classFinder function with the file path
print(classFinder(file_path))
